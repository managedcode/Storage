<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <IsPackable>true</IsPackable>
    </PropertyGroup>

    <!--NuGet-->
    <PropertyGroup>
        <Title>ManagedCode.Storage.Sftp</Title>
        <PackageId>ManagedCode.Storage.Sftp</PackageId>
        <Description>ManagedCode storage provider backed by SFTP over SSH.</Description>
        <PackageTags>managedcode, sftp, storage, ssh, blob, file</PackageTags>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="..\..\ManagedCode.Storage.Core\ManagedCode.Storage.Core.csproj" />
    </ItemGroup>

    <ItemGroup>
        <PackageReference Include="ManagedCode.Communication" />
        <PackageReference Include="Humanizer.Core" />
        <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
        <PackageReference Include="SSH.NET" />
    </ItemGroup>

</Project>
